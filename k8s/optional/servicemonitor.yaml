apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: effintrak-app
  labels:
    app: effintrak
spec:
  selector:
    matchLabels:
      app: effintrak
      component: backend
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
